<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SEND_SMS" tools:ignore="SmsAndCallLogPolicy"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:ignore="ForegroundServicesPolicy"/>
    <uses-permission android:name="android.permission.RECEIVE_SMS" tools:ignore="SmsAndCallLogPolicy"/>
    <uses-permission android:name="android.permission.READ_SMS" tools:ignore="SmsAndCallLogPolicy"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="SmartPillCare"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smartpillcare"
        tools:targetApi="31">

        <!-- Launcher Activity -->
        <activity android:name=".GetStartedActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Other Activities -->
        <activity android:name=".LoginActivity" android:exported="true"/>
        <activity android:name=".MainActivity" android:exported="true"/>
        <activity android:name=".PatientActivity" android:exported="true"/>
        <activity android:name=".MedicineActivity" android:exported="true"/>
        <activity android:name=".AppointmentActivity" android:exported="true"/>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".GeminiChatActivity"
            android:exported="true" />



        <!-- Broadcast Receivers -->
        <receiver android:name=".ReminderReceiver" android:enabled="true" android:exported="true"
            android:permission="TODO" />
        <receiver android:name=".MissedDoseReceiver" android:exported="true"
            android:permission="TODO" />
        <receiver android:name=".NotificationActionReceiver" android:exported="true"
            android:permission="TODO" />

        <!-- Foreground Service -->
        <service android:name=".ReminderService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            tools:ignore="ForegroundServicePermission,MissingClass"/>
    </application>
</manifest>
